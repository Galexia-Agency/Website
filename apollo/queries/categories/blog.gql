query Blog {
  posts(where: {categoryName: "Blog"}) {
    nodes {
      title
      uri
      author {
        name
      }
      excerpt
      featuredImage {
        mediaItemUrl
      }
    }
  }
}