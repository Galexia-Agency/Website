query Blog {
  posts(where: {categoryName: "Blog", status: PUBLISH}) {
    nodes {
      title
      author {
        name
      }
      excerpt
      featuredImage {
        mediaItemUrl
      }
      slug
      date
    }
  }
}